---int x = 1;
int[] a = [1, 2 ,3 ,4];

type record {
    int x;
    int y;
}

record tmp;
tmp.x = 1;
tmp.y = tmp.x + 1;

print (a[tmp.x]);

func int foo(int x) {
    return (x + 1);
}

int f = 0;

try {
    int b = 1/f;
}
except {
    print("Divide by zero");
}


print (foo(foo(3)));---

func int bibinacci_rec(int x) {
    if (x==1) {
        return 1;
    } else if (x==2) {
        return 1;
    }
    return bibinacci_rec(x - 1) + bibinacci_rec(x - 2);
}

print(bibinacci_rec(5));

int[] a = [1,1];

func int bibinacci_dp(int x) {
    int i = 0;
    while (i < x) {
        if (i < 2) {
            continue;
        }
    }
}